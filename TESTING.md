# 実機確認チェックリスト（TESTING.md）

iPhone（iOS 17 以降の Safari）で、GitHub Pages に公開した https://trendgoing-stack.github.io/web-manabi/ を開いて確認する。
「Safari」と「ホーム画面から起動」の両方で確かめる項目は、両方に印を付ける。ライトモードとダークモードの両方で読めることも、あわせて見る。

## 検索

- [ ] 日本語（例：`保存`）、カタカナ（例：`サービスワーカー`）、ひらがな（例：`さーびすわーかー`）、英字（例：`canvas`、全角の `ｃａｎｖａｓ`）で検索できる
- [ ] 空白で区切った複数の語（例：`キャッシュ 更新`）で、両方を含む項目だけが出る
- [ ] カテゴリ・レベル・アプリで絞り込める
- [ ] 見つからないときに、カテゴリ一覧と用語集への導線が出る
- [ ] 検索欄をタップしても画面が拡大されない

## 詳細画面・アプリ

- [ ] アプリ詳細から、そのアプリの技術一覧と、各技術の詳細画面に移動できる
- [ ] 詳細画面から戻ると、元の画面のスクロール位置に戻る（ホーム画面から起動したときは、ヘッダーの「＜」で戻れる）
- [ ] 本文の下線の付いた用語（`[[用語]]`）をタップすると、ポップアップで意味が表示され、「〜を見る」で詳細に移動できる
- [ ] コード抜粋の「この行を GitHub で見る」で、記載した行がハイライトされた状態で開く
- [ ] 「最新版を見る」で、main ブランチのファイルが開く
- [ ] コード抜粋の「コピー」でコピーでき、ほかのアプリに貼り付けられる
- [ ] 長いコードの行は、抜粋の中だけ横にスクロールでき、ページ全体は横に動かない
- [ ] 外部リンクが新しいタブで開き、外部リンクのアイコンが付いている

## ミニデモ

- [ ] Canvas API：画像を選ぶと、縮小前後の大きさとファイルサイズ、縮小した画像が表示される
- [ ] crypto.getRandomValues：「サイコロをふる」で 2 つの分布が表示される
- [ ] Web Share API：共有シートが開き、テキストをほかのアプリに渡せる（キャンセルしても問題ない）
- [ ] Pointer Events：指で線が描け、描いている間ページがスクロールしない
- [ ] Web Audio API：ボタンで音が鳴る（消音モードでは鳴らないことがある）

## 学ぶ

- [ ] クイズとフラッシュカードで、すべての項目が出題の対象になっている（範囲「すべて」で 55 件）
- [ ] 回答すると正誤と解説、詳細画面へのリンクが出る。10 問で結果が表示される
- [ ] 範囲（カテゴリ・アプリ）を選べ、項目が 4 件未満の範囲（例：外部サービス）では「出題できません」と表示される
- [ ] 「このアプリの技術でクイズ」から、そのアプリの範囲で出題される
- [ ] 「今日の復習」が翌日以降に正しく出題される（1 問間違えてから端末の日付を翌日に変えると、学ぶタブの件数が増える。確認後は日付を元に戻す）
- [ ] フラッシュカードで、裏面の左右スワイプで「まだ」「覚えた」を選べる
- [ ] フラッシュカードの上から縦にスクロールしても、ページが普通にスクロールする（スワイプに邪魔されない）
- [ ] マイの学習状況で、箱ごとの件数と正答率が回答に合わせて変わる

## マイ

- [ ] お気に入りの追加・解除、並べ替えができる
- [ ] 閲覧履歴に見た項目が新しい順に並び、「すべて消す」で消える
- [ ] メモを書いて別の画面に移り、戻ってもメモが残っている。メモ欄をタップしても画面が拡大されない
- [ ] 「メモを付けた項目」に一覧が出る

## データ管理

- [ ] エクスポートで JSON ファイルを保存できる
- [ ] **Safari でエクスポートしたデータを、ホーム画面から起動したアプリでインポートできる**（統合・置き換えの両方）
- [ ] 形式の違うファイルをインポートすると、何も変わらずにエラーが表示される
- [ ] 「すべてのデータを削除」で、確認ダイアログのあとにデータが消える
- [ ] 確認ダイアログや用語のポップアップが、ホーム画面から起動したときも画面の中央に出る

## 表示

- [ ] ライトモードとダークモードの両方で、文字・バッジ・正誤の色が読める
- [ ] 文字サイズ「大」で文字が大きくなり、レイアウトが崩れない
- [ ] 画面の上下の切り欠き・ホームバーに、見出しやタブバーが隠れない（safe-area）
- [ ] タップできるボタンやリンクが小さすぎない

## PWA・オフライン

- [ ] ホーム画面に「Webまなび帳」の名前とアイコンで追加される
- [ ] ホーム画面から起動すると、アドレスバーのない全画面で開く
- [ ] **機内モードでホーム画面から起動し、全タブ（探す・アプリ・学ぶ・マイ・設定）、詳細画面、クイズ、フラッシュカード、ミニデモが動く**
- [ ] 機内モードで外部リンクを開くと開けない（アプリ自体は止まらない）
- [ ] **Service Worker の更新が反映される**：`sw.js` の `VERSION` を上げて push したあと、アプリを開き直すと「更新があります（タップで再読み込み）」が表示され、タップで新しい版になる（設定画面のバージョンで確認）

## アクセス解析（GoatCounter）

- [ ] 設定の「アクセス解析を送信しない」をオンにして開き直すと、`count.js` が読み込まれない（PC の開発者ツールのネットワークで、`km-apps.goatcounter.com` への送信がないことを確認）
- [ ] オンのときも、送信は起動時の 1 回だけで、パスが `/web-manabi/` になっている（画面を移動しても送信されない）
- [ ] 機内モードでもエラーが表示されない

## ヘルプ

- [ ] 設定 → ヘルプに、オリジナルの要約であること、「要再確認」の意味、コード抜粋の時点、保存データが別になること、アクセス解析で送る内容が書かれている
- [ ] 以前の版で「未確認項目の表示」を切り替えていても、すべての項目が表示される
